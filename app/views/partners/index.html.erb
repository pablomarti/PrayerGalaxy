<h1>Listing partners</h1>

<table>
<% @partners.each do |partner| %>
  <tr>
    <td><%= partner.image %></td>
    <td><%= partner.description %></td>
  </tr>
  <tr>
    <td colspan="2"><%= link_to 'Edit', edit_partner_path(partner) %></td>
  </tr>
<% end %>
</table>