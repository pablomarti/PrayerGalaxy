<div class="productItem left">
	<div class="productItemPic">
		<%= image_tag item.image %>
	</div>

	<div class="productItemContent" style="height:60px">
		<span class="productItemContentTitle"><%= truncate(item.name, :length => 18) %></span>

		<div class="productItemContentDescription">
			<%= truncate(item.description, :length => 25) %>
		</div>

		<div class="bigPrice">
			<%= number_to_currency(item.price, :unit => "$") %>
		</div>
	</div>
		
	<div class="buyNowButton" style="padding-left:60px;">
		<%= link_to image_tag("buyNowButton.png"), store_product_path(item.id), :style => "background:none;" %>
	</div>
</div>