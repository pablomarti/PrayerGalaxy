<h1>Mission</h1>

<table>
  <tr>
    <th>Description</th>
    <th></th>
  </tr>

<% @missions.each do |mission| %>
  <tr>
    <td><%= mission.description %></td>
    <td><%= link_to 'Edit', edit_mission_path(mission) %></td>
  </tr>
<% end %>
</table>

<br />
