<h1>About</h1>

<table>
<% @abouts.each do |about| %>
  <tr>
    <td><%= image_tag about.image %></td>
    <td><%= about.description %></td>
  </tr>
  <tr>
    <td colspan="2"><%= link_to 'Edit', edit_about_path(about) %></td>
  </tr>
<% end %>
</table>
