<%= form_for(@admin_level) do |f| %>
  <% if @admin_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_level.errors.count, "error") %> prohibited this admin_level from being saved:</h2>

      <ul>
      <% @admin_level.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :level %><br />
    <%= f.text_field :level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
